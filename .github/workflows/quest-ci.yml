# Purpose: pull request validation that is reliable on GitHub-hosted runners.
# This workflow checks repository conventions and builds a Quest Android smoke APK
# to prove the project imports, compiles, and packages for the target platform.
# Full EditMode and PlayMode tests are intentionally local-only because the
# UnityCI Linux Android test runner is not reliable for local LAN PlayMode tests
# and has produced post-test Docker exit 139 failures after valid XML output.
name: Quest CI

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  repository-checks:
    name: Repository checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          lfs: true

      - name: Pull Git LFS assets
        run: git lfs pull

      - name: Check shell script syntax
        run: |
          bash -n scripts/unity/*.sh
          bash -n scripts/server/install.sh
          # The admin helper is /bin/sh on purpose -- it runs on a minimal server where bash may
          # not be installed -- so check it with sh, which is the stricter grammar here.
          sh -n scripts/server/blockiverse-server-admin.sh

      - name: Check release workflow conventions
        run: |
          grep -Eq '^[0-9]+\.[0-9]+\.[0-9]+$' ProjectSettings/BlockiverseVersion.txt
          grep -q '0.1.0-alpha.run318.4.663f074' docs/adr/0005-release-versioning.md
          grep -q 'alpha -> beta' docs/architecture/branching-and-release.md
          grep -q 'beta -> rc' docs/architecture/branching-and-release.md
          grep -q 'rc -> store' docs/architecture/branching-and-release.md
          test -f .github/workflows/quest-ci.yml
          test -f .github/workflows/quest-alpha.yml
          test -f .github/workflows/quest-promote.yml
          test -f .github/workflows/server-release.yml
          test ! -f .github/workflows/meta-release.yml
          # The release archive stages these by name. A rename that only updated the workflow would
          # produce a release whose install path silently vanished.
          test -f scripts/server/install.sh
          test -f scripts/server/blockiverse-server.service
          test -f scripts/server/blockiverse-server-admin.sh
          test -f docs/server/README.md
          test -f docs/server/configuration.md
          test -f docs/server/security-posture.md
          test -f SERVER-EULA.md
          # MemoryDenyWriteExecute breaks the Mono JIT the server runs on; the unit says so.
          grep -q 'DELIBERATELY ABSENT: MemoryDenyWriteExecute' scripts/server/blockiverse-server.service

  unity-validation:
    name: Unity validation
    needs: repository-checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          lfs: true

      - name: Pull Git LFS assets
        run: git lfs pull

      - name: Restore Unity Library cache
        uses: actions/cache@v4
        with:
          path: Library
          key: unity-library-ci-${{ runner.os }}-Android-6000.5.8f1-${{ hashFiles('ProjectSettings/ProjectVersion.txt', 'ProjectSettings/BlockiverseVersion.txt', 'Packages/manifest.json', 'Packages/packages-lock.json') }}
          restore-keys: |
            unity-library-ci-${{ runner.os }}-Android-6000.5.8f1-
            unity-library-ci-${{ runner.os }}-Android-

      - name: Compute smoke build settings
        id: settings
        run: |
          base_version="$(tr -d '[:space:]' < ProjectSettings/BlockiverseVersion.txt)"
          if ! printf '%s' "$base_version" | grep -Eq '^[0-9]+\.[0-9]+\.[0-9]+$'; then
            echo "ProjectSettings/BlockiverseVersion.txt must contain MAJOR.MINOR.PATCH without a leading v." >&2
            exit 64
          fi

          short_sha="$(git rev-parse --short=7 HEAD)"
          version_name="${base_version}-ci.run${GITHUB_RUN_NUMBER}.${GITHUB_RUN_ATTEMPT}.${short_sha}"
          version_code="$(( $(date -u +%s) - 1577836800 ))"
          apk_filename="BlockiverseVR-${version_name}-ci.apk"
          output_path="Builds/CI/${apk_filename}"

          echo "apk_filename=$apk_filename" >> "$GITHUB_OUTPUT"
          echo "output_path=$output_path" >> "$GITHUB_OUTPUT"
          echo "version_code=$version_code" >> "$GITHUB_OUTPUT"
          echo "version_name=$version_name" >> "$GITHUB_OUTPUT"

      - name: Free runner disk space for Unity Android image
        if: runner.os == 'Linux'
        run: |
          df -h
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android/sdk
          sudo rm -rf /opt/ghc
          sudo rm -rf /usr/local/.ghcup
          sudo rm -rf /usr/local/share/boost
          sudo rm -rf "$AGENT_TOOLSDIRECTORY"
          docker system prune --all --force
          df -h

      # This smoke build is the GitHub-hosted Unity validation gate. It catches
      # package import, compile, Android target, and APK packaging failures.
      - name: Build Android smoke APK
        uses: game-ci/unity-builder@v4
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
        with:
          unityVersion: 6000.5.8f1
          targetPlatform: Android
          buildMethod: Blockiverse.Editor.BlockiverseBuildSmoke.BuildDevelopmentAndroid
          versioning: None
          customParameters: >
            -blockiverseBuildOutput ${{ steps.settings.outputs.output_path }}
            -blockiverseBuildVersionName ${{ steps.settings.outputs.version_name }}
            -blockiverseBuildVersionCode ${{ steps.settings.outputs.version_code }}

      - name: Upload CI APK artifact
        uses: actions/upload-artifact@v7
        with:
          name: blockiverse-ci-${{ steps.settings.outputs.version_name }}
          path: ${{ steps.settings.outputs.output_path }}
          if-no-files-found: error
